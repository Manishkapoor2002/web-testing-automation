# web-testing-automation

This project implements a Playwright TypeScript framework for automating UI tests, including tests for alert functionality on the Alert page.

## Features
- Test Alert functionality with various scenarios.
- Structured directory for tests, page objects, and API interactions.
- Easy to extend and maintain.

## Getting Started
- Clone the repository.
- Install dependencies with `npm install`.
- Run tests using `npx playwright test`.
